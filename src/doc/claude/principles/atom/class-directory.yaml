name: "Class = Directory + Method-per-file"
description: "Every class must be a directory with each method in a separate file, following the AKAO.io pattern for maximum reusability and maintainability."
critical: true